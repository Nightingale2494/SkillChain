import { Badge, Button, Card } from "@/components/ui";
import { AppShell } from "@/components/shared/app-shell";
import { scholarships } from "@/lib/mock-data";
export default function MilestonesPage() { const milestones=scholarships.flatMap(s=>s.milestones.map(m=>({...m, scholarship:s.title, student:s.studentName}))); return <AppShell title="Milestone verification" subtitle="Review proofs, approve or reject submissions, and trigger Soroban escrow releases after academic validation."><div className="space-y-4">{milestones.map(m=><Card key={`${m.scholarshipId}-${m.id}`}><div className="flex flex-wrap items-center justify-between gap-4"><div><Badge>{m.status}</Badge><h2 className="mt-3 text-xl font-bold">{m.title}</h2><p className="text-slate-300">{m.student} • {m.scholarship}</p><p className="mt-1 text-sm text-slate-400">Verifier: {m.verifier} • {m.amount} XLM</p></div><div className="flex gap-3"><Button variant="secondary">Reject</Button><Button>Approve + release</Button></div></div></Card>)}</div></AppShell>; }
