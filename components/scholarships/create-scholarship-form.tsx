"use client";
import { useState } from "react";
import { Button, Card } from "@/components/ui";
const fields = ["title", "studentName", "university", "country", "category", "goal", "deadline"];
export function CreateScholarshipForm() { const [loading,setLoading]=useState(false); const [done,setDone]=useState(false); return <Card><form className="grid gap-4 md:grid-cols-2" onSubmit={(e)=>{e.preventDefault();setLoading(true);setTimeout(()=>{setLoading(false);setDone(true)},700)}}>{fields.map(f=><label key={f} className="text-sm text-slate-300 capitalize">{f.replace(/([A-Z])/g," $1")}<input required className="mt-2 w-full rounded-2xl border border-white/10 bg-ink/70 px-4 py-3 text-white" placeholder={f}/></label>)}<label className="text-sm text-slate-300 md:col-span-2">Description<textarea required className="mt-2 min-h-32 w-full rounded-2xl border border-white/10 bg-ink/70 px-4 py-3 text-white" placeholder="Describe the academic need and verification plan"/></label><Button className="md:col-span-2">{loading ? "Creating Soroban escrow..." : "Create scholarship campaign"}</Button>{done ? <p className="text-mint md:col-span-2">Campaign drafted, Firestore metadata saved, and ScholarshipFactory transaction prepared for signature.</p> : null}</form></Card>; }
