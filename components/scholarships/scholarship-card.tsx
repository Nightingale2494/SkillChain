import Link from "next/link";
import { Badge, Card } from "@/components/ui";
import type { Scholarship } from "@/lib/types";
export function ScholarshipCard({ scholarship }: { scholarship: Scholarship }) { const pct = Math.min(100, Math.round((scholarship.raised / scholarship.goal) * 100)); return <Link href={`/scholarships/${scholarship.id}`}><Card className="h-full transition hover:-translate-y-1 hover:border-mint/40"><div className="flex items-center justify-between"><Badge>{scholarship.category}</Badge><span className="text-xs text-slate-400">{scholarship.status}</span></div><h3 className="mt-4 text-xl font-bold">{scholarship.title}</h3><p className="mt-2 text-sm text-slate-300">{scholarship.studentName} • {scholarship.university}</p><p className="mt-3 line-clamp-2 text-sm text-slate-400">{scholarship.description}</p><div className="mt-5 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-aqua to-mint" style={{ width: `${pct}%` }}/></div><p className="mt-3 text-sm text-slate-300">{scholarship.raised} / {scholarship.goal} XLM funded</p></Card></Link>; }
