import type { ActivityItem } from "@/lib/types";
export function ActivityTimeline({ items = [] }: { items?: ActivityItem[] }) { if (!items.length) return <p className="rounded-2xl border border-white/10 bg-white/[.04] p-4 text-sm text-slate-400">No live activity yet. Wallet, donation and milestone events will appear here in real time.</p>; return <div className="space-y-4">{items.map((item) => <div key={item.id} className="rounded-2xl border border-white/10 bg-white/[.06] p-4"><p className="font-semibold">{item.title}</p><p className="text-sm text-slate-300">{item.description}</p><p className="mt-2 text-xs text-slate-500">{item.timestamp}</p></div>)}</div>; }
